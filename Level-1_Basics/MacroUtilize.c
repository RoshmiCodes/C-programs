#include <stdio.h>
#define P printf
int main()
{
    P("\nThis program implements the declaration and definition of MACRO.");
    P("\nThis is another application of the preprocessor directive in C.");
    P("\nThe required function is given a name by an identifier.");
    P("\nThe identifier then can be used everywhere in the program to access the function.");
    P("\nNo memory space is allocated.");
    P("\nThe control identifies the function before the compilation process.");
    P("\nMACRO name should always be in caps.");
}